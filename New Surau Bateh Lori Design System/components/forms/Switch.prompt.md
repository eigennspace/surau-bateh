Sakelar untuk preferensi yang langsung berlaku.

```jsx
<Switch label="Pengingat adzan" defaultChecked />
```

Aktif memakai teal (`--status-active`), bukan maroon — maroon disediakan untuk aksi, bukan status.
