# Postgres terkelola, terpisah dari Sanity (lihat ADR 0015 di root repo)
DATABASE_URL=postgres://user:password@localhost:5432/absen_magang

# Secret untuk menandatangani session cookie login Pengurus. Generate acak,
# mis. `openssl rand -base64 32`.
SESSION_SECRET=ganti-dengan-string-acak-yang-panjang
